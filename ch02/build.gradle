subprojects {
    apply plugin:'groovy'
    apply plugin:'eclipse'

    repositories {
        mavenCentral()
    }

    task wrapper(type:Wrapper) {
        gradleVersion = '1.9'
    }

    dependencies {
        compile 'org.codehaus.groovy:groovy-all:2.2.1'
    }
}
